<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ridlo.resto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_resto_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_resto_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgetActivity"></activity>
        <activity android:name=".EditPaketActivity" />
        <activity android:name=".AddPaketActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".ProsesActivity" />
        <activity android:name=".MainUserActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".PesanActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".EditMakananActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".AddMakananActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>